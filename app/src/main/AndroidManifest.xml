<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bocadillo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bocadillo">
        <activity android:name=".Chat"></activity>
        <activity android:name=".PopUpContactos"></activity>
        <activity
            android:name=".Pestanas"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.Bocadillo.NoActionBar" />
        <activity android:name=".AnadirContacto" />
        <activity android:name=".SignIn" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>